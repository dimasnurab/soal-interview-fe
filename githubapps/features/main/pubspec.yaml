name: main
description: Module for main
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
